package algorithms.fibonacci;

public class SetValuePairwiseSum {
    /*
    Eg: Find a pair that will sum to the required value
    [1,2,5,9,4] -> Sum 8
    [3,7,4,4] -> Sum 8

    Return Payload - Yes/No, Indexes, Values, Count of pairs

    Input:
        - How is input presented to us ? - In-memory or external source like file, db, buffer, network
        - What are input data structure limitations ? - Array or List.
        - Is there a limit on size of input ? - Memory
        - What data type of values ? - integers.
        - What is the range of values ? - positive, negative, zero.
        - What is the pattern of values ? - sorted, unsorted, allow duplicates.
        -
    Output:
        - Payload - boolean, integer for indexes, integer for values, short integer (16 max) for count of pairs.
        - Format in which output is presented back to client.

    Constraints:
        - Space limits, Time limits, Problem statement, Memory overflow, parallel processing, distributed.
    Test:
        - Test data producer.
     */
    public static void main(String[] args) {
        System.out.println("Hello");
    }
}
