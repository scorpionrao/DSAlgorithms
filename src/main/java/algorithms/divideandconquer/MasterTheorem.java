package algorithms.divideandconquer;

public class MasterTheorem {

    /*

    T(n) = a T(n/b) + O(n^d)

    T(n) = O(n^d)           d > log a base b --> major work at top of tree

    T(n) = O(n^d log n)     d = log a base b --> same work at all levels

    T(n) = O(n^log a)       d < log a base b --> major work at leaves

     */
}
