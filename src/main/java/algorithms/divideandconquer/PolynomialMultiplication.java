package algorithms.divideandconquer;

public class PolynomialMultiplication {

    /*

        MultiplyPoly(A, B) {

            P = 1D array of size [2n-1]
            for i = 1 to 2n-2
                P[i] = 0;

            for (i = 1 to n)
                for ( j = 1 to n)
                    P(i+j) += A[i] + B[j]
        }
        return P

        Runtime : O(n^2)

        T(n) = 4 T(n/2) + O(n)

        Log a to base b = log 4 to base 2 = 2
        d = 1

        O(n ^ log a to base b) = O(n^2)

     */
}
